@model BlogEngine.Models.Page
@{
    ViewBag.Title = "StaticPage";
}

<h2>@Model.Title</h2>
<div class="row">
    <div class="col-lg-8">
        @Html.Raw(Model.Content)
    </div>
    <div class="col-lg-4">
        @{Html.RenderAction("SidePanel", "Home", new List<BlogEngine.Models.Hashtag>()); }
    </div>
</div>
